# Phase 1 — decisions taken autonomously

The user asked for phase 1 (hardening plan, then search-protocol plan) to be executed end to end without
check-ins, with every decision that would otherwise have been theirs recorded here with its reason and
what it costs if wrong. Rulings that change a measured number are also in `docs/phase0-execution/sdd-ledger.md`
with a `ledger.log_event`; this file is the complete list, including the ones that do not.

Plans: `docs/superpowers/plans/2026-09-05-phase1-hardening.md`, `docs/superpowers/plans/2026-09-05-phase1-search-protocol.md`.
Execution: subagent-driven, one implementer per task (opus for transcription-grade tasks, fable for integration or
design), one reviewer per task, a whole-branch review at the end of each plan.

| # | Date | Decision | Why | Cost if wrong |
|---|---|---|---|---|
| D1 | 2026-09-05 | Work in place on branch `phase1-hardening` (then `phase1-search`), no git worktree. | The runbook steps (re-base catch-up, submissions re-ingest, ticker-map coverage, calibration grid, quarantine diagnosis) need this checkout's gitignored `data/` warehouse; `config.data_root()` defaults to `<repo>/data`. `main` is protected by the branch and the PR. | A stray edit lands on the branch; caught by per-task review and the PR. |
| D2 | 2026-09-05 | Task 1's Step-5 mutation check (`if False:` on the empty-symbols guard) is skipped as non-discriminating. | With the guard removed `read_bars(symbols=[])` returns an empty frame and `run` still writes nothing; the behavioural test already pins the contract. | None — identical semantics either way. |
| D3 | 2026-09-05 | Task 7's unit-test fixture points `tickers.OVERRIDES_PATH` at a non-existent file; only `test_the_shipped_overrides_file_parses` reads the real overrides CSV. | The plan's tests compare exact interval sets and expect `override: 0`, but the shipped CSV carries the BBBY row; without the fixture every build test would see it. | An override that breaks `build()` is caught only by the parse test — acceptable. |
| D4 | 2026-09-05 | Both plans are executed in this session, hardening first, search protocol second, with no pause for sign-off between them. | The user asked for the entire phase-1 implementation autonomously. Neither plan registers a hypothesis or spends a holdout, so the gate discipline in CLAUDE.md is untouched. | If the user wanted to review the hardening PR before the search work started, the second branch is rebased or re-opened — bounded rework. |
| D5 | 2026-09-05 | The 2-minute status loop the user asked for is run as asked, even though this session's prompt cache has a one-hour TTL and a 20–30 minute cadence would keep it warm. | Explicit instruction; the user owns the budget. | Extra cache-read turns; no correctness effect. |
| D6 | 2026-09-05 | PR creation and merge: each plan's branch is pushed and a PR opened when its final review is clean, but **merging is left to the user** (a push to `main` is the one outward-facing action the process reserves for a person). | CLAUDE.md's workflow is PR → squash-merge; the Codex review hook runs on `gh pr create`. The search-protocol branch is cut from the hardening branch so it does not wait on the merge. | The second PR shows the first's commits until the first merges; squash-merging in order resolves it. |
| D7 | 2026-09-05 | Quasar deployment steps (image rebuild and import, secret patch, PVC sync, `rebase-catchup` Job) are deferred until the hardening PR is merged; the MacBook copy of the warehouse gets every runbook step now. | Deploying an unmerged branch's image to the always-on cluster would run unreviewed code against the production PVC; the nightly there keeps running phase-0 code meanwhile, which is safe. | Splits between now and the deploy are caught by the seven-day lookback on the first post-deploy night, and by a wider `--from` if the gap is longer. |
| D8 | 2026-09-05 | Ruling 43 amended after review: a break whose next close returns to within the band of the pre-break close is a one-row spike and is dropped (neither it nor the revert row counts as a break); the unconfirmed-final-row rule then applies to what remains. | The plan's rule as written regressed one nightly after the case it fixed: the reverting print was dropped as unconfirmed and the junk print became a confirmed break, leaving the junk price as the whole series. The spike rule fixes that and the strictly-inside one-day spike the plan had left open. | A genuine one-day 5× move that fully reverts the next day loses that day's close; both vendors agreeing on such a print has not been observed. An alternating junk series collapses to its first row. |
| D9 | 2026-09-05 | Ruling 44 amended before the ticker map's first real build: a rename bounds the new symbol's interval at the rename date only when the store's series for that symbol actually begins there; if bars predate the rename, the vendor served the company's lineage under the new symbol and the interval keeps its open start. | A read-only probe of the real warehouse showed both vendors key the backfilled history by the *current* symbol (NXH carries the OSTK→BYON→BBBY→NXH lineage continuously from 2016; 1,473 of 1,556 rename targets have bars before their rename date). Applied as planned, the map would have removed the pre-rename history of every renamed company from the universe and the fundamental signals. | If a vendor ever splices by symbol string, that series is attributed to the current owner for its whole length; the coverage measurement, the calibration re-run and the break detector are the counters. Dead companies whose symbol was later reused are absent from the store, not misattributed. |
| D10 | 2026-09-05 | The ticker map's rename and merger rules are gated on the store's own series spans (with D9): a rename bounds a holder only where the symbol's stored series begins at the rename; a rename-derived interval for the old symbol is dropped where another holder's stored series predates the rename; a merged symbol whose series keeps printing past the merger is a re-listing regardless of which source its row came from. A failed SEC map refresh no longer fails the nightly. | Review found the plan's rules left other holders of the old symbol unbounded (duplicate symbol-days) and keyed the merger re-list test on the row's source; with lineage-keyed history the only honest arbiter is the store. | Where the store genuinely holds two companies under one symbol string, the current owner is attributed the whole series; the break detector and the coverage and calibration numbers are the counters. |
| D11 | 2026-09-05 | `issuance.signal` split-adjusts the year-ago share count onto the current count's basis (splits with an ex-date between the two filings), default on, with a driver switch that reproduces the ruling-40 numbers; run as a fifth calibration experiment. | The OSAP definition uses `shrout × cfacshr` (split-adjusted); ours used as-filed counts, so a 2:1 split read as 100% issuance — 977 splits on 829 symbols fall inside the development window. | If a filer restates the earlier count on the new basis, the adjustment double-counts that split; the calibration delta and a count of such filers are the counters. |
| D12 | 2026-09-05 | The panel-horizon look-ahead the final review found (break truncation in `metrics.monthly_longshort` and `engine._market_frame` is applied at the panel's *end*, so a 2018 break can erase 2016–2017 rows for a 2017 formation; pre-existing since ruling 30) is registered as a limit, its two docstring claims corrected, and its fix — per-horizon visibility — is the search branch's first prerequisite task with a calibration re-run to quantify the change. | The fix changes every calibration number and needs its own ruling and re-run; exposure is small (a two-vendor-agreed ≥5× break on a >$5, >$1M name) and the direction is known (flatters). | Every fix-round and grid calibration carries it until then; nothing trades on those numbers before the search branch's re-run. |
| D13 | 2026-09-05 | The nightly ingests the trailing week's rename targets (`new_symbol`) alongside the universe, the re-base job re-bases those targets (the lineage lands under the new symbol from both vendors), and map rule 2 drops the inferred old-symbol interval when the new symbol's series predates the rename, so a renamed company is represented once. | Review showed the nightly could never admit a renamed-into or newly listed symbol (the universe needs 63 days of canonical closes), so every renamed company would vanish and phase-2 positions would be force-exited five days after a rename. | An old symbol whose series the nightly keeps ingesting for a week is represented twice until the map rebuild; the map's coverage measurement is the counter. |
| D14 | 2026-09-05 | Fix wave scope before merge: the re-base vendor-empty guard, the filings migration (committed deployment note + replace-not-merge on an old-schema file + a clear scan error), the nightly memory re-measurement, ruling 47 recomputed on current verdicts with the constant-ratio mechanism named and registered, the §12.1 attribution caveat, ruling 42's order string, and D13. The `process_date` vendor-semantics check is a search-branch prerequisite. | The reviewer's must-fix list; everything else is recorded as deferred with a follow-up list. | A deferred minor turns out load-bearing — the follow-up list is in the ledger and the PR description. |
| D15 | 2026-09-05 | The nightly's `requests: 2Gi` pin stays although the re-measured peak is 2.01 GiB — 9.9 MiB over the plan's "stay under the manifest's 2Gi" constraint, which the re-review flagged as a literal breach. | The plan's own sizing rule (Task 4 step 5: below 1.0 GiB set 1Gi/2Gi, otherwise leave both) governs; the request is a scheduling hint on a 32 GB single node and `limit: 4Gi` is the ceiling that matters; the overshoot is documented in the manifest and its test. | The node fills up and the scheduler under-reserves by 10 MiB — one line in `deploy/nightly-cronjob.yaml` (request 3Gi) if the constraint is to be read literally. |
| D16 | 2026-09-05 | Engine rename rule (ruling 45 amended after PR #5's external review found that a rename could move a held position into another issuer's series on a recycled ticker): a rename carries a position only when the old symbol has stopped printing, the position's last close is on or before the process date, and the two series' log returns agree over the last ≤ 6 common sessions before the rename (`RENAME_DRIFT` 0.0025). | Measured on the real warehouse: 100 of 2,864 renames have the old symbol printing through the rename with the new symbol also live (IR→TT, META→METV; BBT→TFC inside the development window). The first fix (a 30-day lineage gate mirroring the ticker map) would also have blocked 45 of 69 genuine ticker changes whose new-symbol history the vendor backfilled (PPDF→FINV, DGSE→ELA in the window); the return-agreement test separates the two populations with zero misclassification (same-issuer ≤ 0.00064, recycled ≥ 0.0127). | A recycled ticker with no pre-rename overlap is guarded by the quote test alone: a missing vetted close on the due session carries the position anyway — ≈1.6 expected occurrences 2016–2026, ≈0 in the development window, visible as `engine.rename` events. Alternative rejected: quote guard only (same residual, no defence in depth) or the 30-day gate (two genuine dev-window carries lost). |

## Follow-ups (deferred minors from the task and final reviews)

Every "later" item from the per-task reviews and the whole-branch review, one line each, in the order the
execution ledger (`.superpowers/sdd/2026-09-05-phase1-hardening/progress.md`) recorded them. None changes a
measured number; each is a cleanup, a test sharpening, or a check to run before the thing it guards is
leaned on.

- T1: `reconcile._check_symbols` uses O(n²) membership where the fetchers use a seen-set; a third verbatim copy of the symbol normaliser (extract a shared `tbot.jobs`/warehouse helper).
- T1: `test_run_rejects_a_bare_string_symbol_list` lacks `match=`; `.strip()` is unpinned (use `" aapl "`).
- T2: `actions.TYPES` literal is no longer pinned by a test (the assertion is tautological); the `name_changes` sort is unpinned by the fixture (add an early-date, late-alpha row).
- T2: the mergers-dedupe `kind` comment claims unverified vendor behaviour; the module and `fetch_all` docstrings overstate the placeholder guard (renames/mergers only); the acquirer "null for a cash deal" comment is not enforced; `_SORT["mergers"]` is not a total order (plan-mandated).
- T3: `rebase._symbols` near-duplicates `nightly._as_symbols` (five normalisers now); the nightly order test does not pin the argument to `symbols_to_rebase`; `test_main_defaults_to_to_yesterday` depends on the wall clock; the vendor-failure test name overclaims now that three collaborators follow the vote.
- T3: `symbols_to_rebase` has no cap or chunking for a wide `--from`; the `test_rebase` reconcile fake is looser than the keyword-only real signature.
- T4: the `read_filings` recorder test pins the last call, not the only call; `tuple(forms)` normalisation is cosmetic; 48 per-asof memo entries in a calibration walk — memory better, wall time unmeasured.
- T5: the `read_canonical` `max_jump` paragraph wraps awkwardly (`reconcile.py` ~293); a null-neighboured spike is unremovable (unreachable through the vote).
- T6: `"name": ""` would write a blank entity; a former name with unparseable `from`/`to` is an open window (map consumers must treat a null `from` explicitly); the empty-warehouse test is not extended with `read_entities`; two lines over 90 characters.
- T7: rule 1 cannot tell a lineage from a symbol-string splice — the override list is the control (stated in ruling 44); `test_a_rename_chain_dates_every_symbol_of_one_filer` uses synthetic chain dates while `CHAIN` and the CSV use the real ones; a ticker listed under two CIKs in the SEC map propagates its duplicate into inferred old rows.
- T8: 60 real `(symbol, process_date)` pairs carry more than one merger kind — the cash-first tie-break is live; count on the development window before merger exits are leaned on.
- T8: `read_name_changes` is unpacked positionally; a rename resets the gap counter (up to 2× tolerance across a rename); the exits paragraph has a ragged line (`engine.py` ~68); the self-rename test docstring overstates what it discriminates.
- T8 → search branch: `process_date` vendor semantics (first non-trading day vs last trading day) — one `reason` count on a real-warehouse engine run (38 dev-window mergers, 23 renames); registered in report §12.6.
- T9: `events.window()`'s filed prefilter (`asof − (days + 1) .. asof`) drops a filing whose EDGAR `filingDate` is the next business day after an after-close acceptance when `asof` falls on the weekend between — conservative direction; widen the right bound to `asof + 3` or record as a limit before a signal consumes `window`.
- T9: redundant `is_null` disjunct in `after_close`; unclosed `<script>` bodies survive stripping; no non-200 fetch test; the pacing test patches the global `time` module; dead test imports; `score` can return `-0.0`; `USER_AGENT_ENV` duplicated in `edgar.py` and `tickers.py`.
- T10: the issuance split window is keyed on filing dates (period end would be tighter — ruled); a rename after a split leaves the split under the old symbol (map-at-asof misses it — the same lineage caveat as §12.1); `read_splits` is uncached and re-read per formation; no positive `--split-adjust` flag; the zsh warning sits after the loop; round-1 RED evidence is a summary line.
- T11: the symbols counter sits outside the bucket loop; `tools/t17/README.md` has no `quarantine_by_month.py` row; `infer_schema_length` on a wider window. (The prose slips — 28 vs 29 names, days vs events, the 6.36% floor, the "sub-2%" headline, the missing deviation sentence and the conditional two-source line — were fixed in the fix wave; the zero alpaca close is guarded in the floor statistics of the third run.)
- Final review → search branch: the panel-horizon truncation in `metrics.monthly_longshort` and `engine._market_frame` (D12; report §12.6) with a calibration re-run to quantify it; the vendor-basis floor's per-name corporate action and the spec-A3 basis decision (ruling 47).
- Final re-review → re-base: only a vendor returning *nothing* for a held symbol is guarded (`rebase._pull`); a vendor returning a *truncated* history (fewer rows than the store's own span) still poisons the re-vote over the missing span — add the span-length check before the re-base is leaned on for delisted names.
- Final re-review → `tools/t17/quarantine_by_month.py`: `n_symbols_persistent` changed meaning between diagnosis runs 2 and 3 (events-based 140 → verdict-based 138; the events count moved to `n_symbols_persistent_events`) and `events_seen` is un-suffixed — the prose says so, the payloads do not.
- PR #5 external review (Codex) → engine: the rename rule's residual (recycled ticker, no pre-rename overlap, hole on the due session) and the merger bound left strict while the rename bound is `<=` — both fold into the search branch's `process_date` task; `RENAME_DRIFT`'s margin is quoted in the constant's comment, `_different_issuer`'s docstring and ruling 45 — restate all three together if re-measured.
- PR #5 external review (Codex) → `features/events.py`: `knowable_on` after an early close is a calendar day and can land on a full closure (2024-07-04); the early-close rules encode NYSE's standing schedule, a future schedule change is silent.

